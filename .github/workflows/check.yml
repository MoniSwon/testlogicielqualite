# Run unit tests on push

name: Check

on:
    push:
        branches:
            - develop
    pull_request:
        branches:
            - develop


jobs:
    lint-and-test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - uses: actions/setup-node@v1
              with:
                node-version: '18'
            - run: npm run test
           